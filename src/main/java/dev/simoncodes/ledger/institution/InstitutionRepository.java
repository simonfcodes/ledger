package dev.simoncodes.ledger.institution;

import dev.simoncodes.ledger.common.ReadOnlyRepository;

import java.util.UUID;

public interface InstitutionRepository extends ReadOnlyRepository<Institution, UUID> {
}
